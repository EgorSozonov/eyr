def fizzBuzz = {{}
    for {i' = 1; i < 101; i++; }{
        worker i;
    }
};

def worker = {{ i Int, }
    if  i % 3 == 0 && (i % 5 == 0) {print `FizzBuzz`;}
    eif i % 3 == 0 {print `Fizz`;}
    eif i % 5 == 0 {print `Buzz`;}
    else {print i;} 
};

def main = {{}
    (fizzBuzz);
};
