// Scratchspace for design ideas


////////////////////
// Class system
////////////////////

Structs, interfaces are the basic blocks.

There is no "private", only "protected".

A struct may define also an interface. Interfaces consist of virtual or concrete method decls.
The definitions for those decls must be provided by the struct and can see protected fields of the 
struct

KEY: different kinds of pointers:

- pointer to class allows access to its public fields, methods (which see protected elements) and
functions (which only see public elements) `'Component`
- pointer to interface `^Component`


Inheritance is limited by design. An abstract class may be extended by a concrete class. A concrete
class may be extended by another. And that's it. So maximum height of an inheritance hierarchy = 3.
No "final" classes needed: to make a class final, you just inherit from an empty class. Maybe even
provide ability to inherit from Void type?

Abstract classes: they just don't implement at least one method of their own interface.

Mixins: sharing implementation withou affecting the types. A mixin can have: fields, methods 
(public, protected, virtual). It cannot be cast to, and adding a mixin does no change the type
of a struct. Mixin methods may be overridden in the class

Methods real by default (virtual only with the "virtual" keyword).

"pub" keyword for fields, types, functions (not for methods since those are handled by the 
interfaces - everything in the interface is public, other methods are all protected)

Friends: yes, necessary.

If a class has at least one private field, it is considered opaque and cannot be passed by value.

Centralized syntax to provide all this info?

class Foo {
interface {
   allFieldsPublic; // all fields public, can be constructed with data init or passed by value
   extends Bar; 
} 
}

class Foo {
interface {
   abstract; // can be inherited from, but only by a concrete class
   mixin Counter;
}
}

class Foo {
interface {
   implements Writer;
   mixin CounterMixin;
}
}


Interfaces:

interface Writer {
   write [Self -> Str];
}

class Foo {
interface { // no need to specify the name, it will be same as class
   abstract;
   foo [Self -> Str];
   Writer;
   mixin CounterMixin;
}

constants {
   ENV = `Asdf.bar`;
}
   
   id Int;
   name Str;
   pubField pub Int;
   
   constructor [Int Str -> Self] f{id name ->
      return Foo(id id, name name, pubField 11);
   }
   
   fn foo f{ // no need to specify the type - it's in the interface!
      return name + " " + $id;
   }
   
   fn write f{ // no need to specify the type
      
   }
   
   fn someProtectedMethod [Self Int -> Double] f{ // Protected because not in the interface!
      
   }
   
   fn someStaticMethod[Int -> Double] f{
      
   }
}


////////////////////
// Memory management
////////////////////

Arenas and atomic reference counts.

Type taxonomy:

[flat types | arena-pointing types | refcounted types ]
[flat types | A-types | R-types ]

Flat types: contain no references of any kind.
Arena-pointing types: contain references into some arenas they do not own.
Refcounted types: are behind (atomic) reference counters, may own an arena. Their internal
refs may be either Arcs themselves, or may point into the owned arena (maybe multiple arenas?).


Example: [Foo id Int, value Long] is flat,
         [Foo id Int, name 'a String] is an A-type,
         Arc [Foo id Int, name Arc String, internalData 'Bar] is an R-type.
         
         
A-types may not own an arena because then that arena will never get freed!
R-types may not point into an arbitrary arena because there's no lifetime comparison between
Arc and arbitrary arena. I.e. an Arc may have a dynamic lifetime (it will be deallocated when
no thread and no heap object holds a reference to it), but so can an Arena (since it's Arc itself
and may be returned from functions).
         
R-types may only be referenced by other arc types. But they may contain a single (owned) arena
and contain arbitrary references into that arena.
         
Example:

   r: Arc Foo = getRefcountedObj;
   arenaRef: 'r Bar = r.fld;
   doSomethingWith arenaRef;
   // r's refcount gets decremented, arenaRef is no longer valid, though r may still live on
         

Arena objects are all refcounted.

   
Functions working with arena references must specify arena flow (aka lifetime annotations). For
example:

   def foo = {{x '1 Foo, y '2 Bar -> u '3 Bar} ...}

Here the function specifies that the ref it returns will be in the same arena as its second param.
It may allocate "z" or it may just return a pre-existing ref, but it will be in that arena, and
callers can use that info:

   a = newArena();
   { 
      b = newArena();
      x = [Foo ... @a];
      y = [Bar ... @b];
      u = foo x y; // u has type 'b Bar because in "foo"'s signature, '1 ~ 'a and '2 ~ 'b
   }
   // here, u is invalid because the b arena has been destroyed



Every function may have an implicit call frame arena, which can be referenced as "'0".
It's used for temporary expression results that are not assigned to a local like strings concat.
It is freed on function exit. Of course if it's not used in the body of the function, it is not
created at runtime.
For example, string concatenation:

   c '0 Str = `asdf` + str;
   
Specifying another arena automatically invokes copying from the call frame arena:

   c '1 Str = `asdf` + str; // means: copy 1 (`asdf + str)
   
R-types must always be assigned to local variables, i.e. they may not be intermediate results of
expression evaluation.

Arena subtiming: some arenas must be statically lifetimed. This allows to set upper bounds on them,
to ensure that the outer arena lives longer than them. Useful for fork-join parallelism, for example:

   parentArena = newArena();
   childArena = newStaticArena(); // not refcounted, will definitely get destroyed at end of scope
   forkedThread = newThread childArena; // in there objs in "childArena" can reference "parentArena"
   join forkedThread; // "childArena" gets destroyed but the results of the thread are in "parent"


   [[[Optimization (so for the future)]]]
   
When the types of interface values are statically-known, resolve method calls statically
interface TransactionProcessor {...}
class { p1 FooProcessor; p2 BarProcessor; ...;
   fn process f{
      each { procs [A TransactionProcessor] = [p1 p2 p3] -> procs.@ .method a }
      // ^ here, we know the array statically, and even though members are of different types,
      // they are of the same interface. We can unroll the loop and make the `.method`
      // statically-dispatched.
      // This is a substitute for the pattern of "abstract class and then we call the different
      // implementations in a loop". Maybe we can do away with abstract classes altogether, just
      // plain inheritance of no more than 3 layers?
   }
}
